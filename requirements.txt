# Dependencias necesarias para el proyecto de Algoritmos Genéticos Flow
# Instalar con: pip install -r requirements.txt

# Dependencias principales
colorama>=0.4.4          # Para colores en terminal
numpy>=1.21.0           # Para cálculos numéricos (usado en métricas)

# Dependencias para visualización (opcionales)
matplotlib>=3.5.0       # Para gráficas y visualizaciones
seaborn>=0.11.0        # Para gráficas más elegantes (opcional)

# Dependencias para desarrollo (opcionales)
pytest>=6.0.0          # Para testing
pytest-cov>=2.12.0     # Para cobertura de tests
black>=21.0.0          # Para formateo de código
flake8>=3.9.0          # Para linting

# Para análisis de datos adicional (opcional)
pandas>=1.3.0          # Para manejo de datos tabulares
scipy>=1.7.0           # Para estadísticas avanzadas